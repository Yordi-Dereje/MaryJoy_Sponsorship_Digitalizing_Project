{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for Mary Joy Ethiopia sponsorship system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand",
    "test:watch": "jest --watch --runInBand",
    "test:coverage": "jest --coverage --runInBand",
    "test:sponsors": "jest sponsors.test.js --runInBand",
    "test:documents": "jest documents.test.js --runInBand",
    "test:beneficiaries": "jest beneficiaries.test.js --runInBand",
    "test:guardians": "jest guardians.test.js --runInBand",
    "test:employees": "jest employees.test.js --runInBand",
    "test:sponsorships": "jest sponsorships.test.js --runInBand",
    "test:auth": "jest auth.test.js --runInBand",
    "test:requests": "jest sponsorRequests.test.js --runInBand",
    "db:test": "node -e \"require('./config/database').testConnection().then(console.log)\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "node-fetch": "^2.7.0",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "supertest": "^6.3.3"
  }
}
